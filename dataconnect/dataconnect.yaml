specVersion: "v1"
serviceId: "with-firebase-data-connect"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "with-firebase-data-connect-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
